/*******************************************************************
 * @file field.c
 *
 * @brief Implementação de extração de campo de registrador (Atividade-06).
 *******************************************************************/

#include "field.h"

uint32_t reg_get_field(uint32_t reg, uint8_t shift, uint8_t width)
{
	/* TODO (Atividade-06): extrair `width` bits de `reg` a partir do bit
	 * `shift`, retornando o valor já deslocado pra começar no bit 0.
	 */
	return 0;
}
