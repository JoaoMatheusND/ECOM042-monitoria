/*******************************************************************
 * @file reg.c
 *
 * @brief Implementação de manipulação de bits de registrador (Atividade-05).
 *******************************************************************/

#include "reg.h"

void reg_set_bit(uint8_t *reg, uint8_t bit)
{
	/* TODO (Atividade-05): setar o bit `bit` de *reg sem afetar os outros. */
}

void reg_clear_bit(uint8_t *reg, uint8_t bit)
{
	/* TODO (Atividade-05): limpar o bit `bit` de *reg sem afetar os outros. */
}

bool reg_read_bit(uint8_t reg, uint8_t bit)
{
	/* TODO (Atividade-05): retornar true se o bit `bit` de reg está setado. */
	return false;
}
