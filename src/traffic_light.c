/*******************************************************************
 * @file traffic_light.c
 *
 * @brief Implementação da máquina de estado do semáforo (Atividade-09).
 *******************************************************************/

#include "traffic_light.h"

enum tl_state tl_next_state(enum tl_state current)
{
	/* TODO (Atividade-09): RED -> GREEN -> YELLOW -> RED. */
	return current;
}
