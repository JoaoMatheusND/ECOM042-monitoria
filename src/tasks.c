/*******************************************************************
 * @file tasks.c
 *
 * @brief Implementação do superloop cooperativo (Atividade-10).
 *******************************************************************/

#include "tasks.h"

bool task_a_should_run(int tick)
{
	/* TODO (Atividade-10): true quando tick é múltiplo de 2 (período 2). */
	return false;
}

bool task_b_should_run(int tick)
{
	/* TODO (Atividade-10): true quando tick é múltiplo de 3 (período 3). */
	return false;
}
