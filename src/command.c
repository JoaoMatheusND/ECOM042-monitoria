/*******************************************************************
 * @file command.c
 *
 * @brief Implementação do parser de comando (Atividade-04).
 *******************************************************************/

#include "command.h"

int parse_command(const char *cmd)
{
	/* TODO (Atividade-04): "ON" -> 1, "OFF" -> 0, qualquer outro -> -1. */
	return -1;
}
